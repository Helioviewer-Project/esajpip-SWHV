listen_at =
{
  port = ${SWHV_PORT_JPIP};
  address = "";
};

folders =
{
  images  = "${SWHV_DIR_IMAGE}";
  logging = "${SWHV_DIR_LOG}";
};

connections =
{
  identification_time_out = 3;
  time_out = 60;
  max_number = 500;
};

general =
{
  logging = 1;
  log_requests = 0;
  max_chunk_size = 64000;
};
