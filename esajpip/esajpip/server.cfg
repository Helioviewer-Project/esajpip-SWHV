listen_at =
{
  port = ${ESAJPIP_LISTEN_PORT};
  address = "";
};

folders =
{
  images  = "${ESAJPIP_DIR_IMAGE}";
  caching = "${ESAJPIP_DIR_CACHE}";
  logging = "${ESAJPIP_DIR_LOG}";
};

connections =
{
  time_out = 60;
  max_number = 500;
};

general =
{
  logging = 1;
  log_requests = 1;
  cache_max_time = -1;
  max_chunk_size = 1000;
};
